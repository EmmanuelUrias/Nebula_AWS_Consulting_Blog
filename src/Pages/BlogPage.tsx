import { useParams } from "react-router-dom"

function BlogPage() {
    // use useParams to grab the ID and based off that Id show the Item
  return (
    <div>BlogPage</div>
  )
}

export default BlogPage